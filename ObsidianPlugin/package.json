{
  "name": "drawpad-obsidian-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Obsidian desktop plugin bridge for DrawPad iPad sync",
  "scripts": {
    "build": "tsc --noEmit && node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs development",
    "test": "node --experimental-strip-types --test test/*.test.mjs"
  },
  "dependencies": {
    "bonjour-service": "^1.3.0",
    "lz-string": "^1.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "obsidian": "^1.8.7",
    "typescript": "^5.8.0"
  }
}
